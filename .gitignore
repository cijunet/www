node_modules/
.DS_Store
Thumbs.db
*.log
.workbuddy/

# 构建产物：本地用 node build/build.mjs 生成，提交进仓库由 GitHub Pages 直接部署（CI 改为仅部署）。
# 仅 WWW/ 入库；其他预览/备份/中间变体仍忽略。
docs/
WWW.build/
WWW2/
site/
www2/
www3/
dist/
dist2/
dist3/
dist4/
dist5/
dist6/
WWW_preview/
WWW_preview2/
site_bak_*/

# 数据源（xlsx 全族与 history-raw.json）仅留本地，绝不进公开仓库。
# 含唯一真源 词句数据.xlsx 与各类 backup_*.xlsx 备份。
data/*.xlsx
data/history-raw.json

# 构建期生成的数据指纹快照（每次构建都变，无需入库）
data-fingerprint.json
